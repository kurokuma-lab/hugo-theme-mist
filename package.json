{
  "name": "hugo-theme-mist",
  "version": "1.0.3",
  "description": "Simple Hugo theme that influenced by the astro_theme_typography and hugo-theme-zozo",
  "type": "module",
  "scripts": {
    "test": "echo \"No tests available\" && exit 1",
    "unocss:watch": "unocss 'layouts/**/*.html' -o assets/css/uno.css --watch",
    "unocss:build": "unocss 'layouts/**/*.html' -o assets/css/uno.css",
    "dev": "npm run unocss:watch & hugo server --source exampleSite --themesDir=../.. --watch --buildDrafts --disableFastRender",
    "build": "npm run unocss:build && hugo --source exampleSite --themesDir=../.. --minify --gc"
  },
  "author": "kurokuma",
  "license": "MIT",
  "devDependencies": {
    "@iconify-json/ri": "^1.2.5",
    "@iconify/json": "^2.2.307",
    "@unocss/preset-attributify": "^65.5.0",
    "@unocss/preset-icons": "^65.5.0",
    "unocss": "^65.5.0"
  }
}